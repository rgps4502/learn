建立新分支branch 並切換過去
git checkout -b <分支名稱>


刪除電腦上的branch
git branch -d <分支名稱>
(如果這個branch有推到github上,github上的不會刪除)


查看電腦上所有branch分支 (q離開)
git branch

查看所有的branch分支 包含remote上的
git branch -a

切換branch
git switch <分支名稱>

把目前的分支的起點移到最新進度
git rebase <要rebase到哪個分支>
ex: git rebase master


把目前的分支整併到另一個分支
git merge <要整併回的分支>
完成後當前分支會消失